# Example environment configuration for MISP Instance — Member Org A


# --- Admin / Organisation identity ---
ADMIN_EMAIL=admin@org-a.test
ADMIN_ORG=OrgA

# --- Networking ---
BASE_URL=https://localhost:8443
CORE_HTTPS_PORT=8443

# --- Database ---
# Use a strong unique password locally; do not reuse across instances.
MYSQL_PASSWORD=

# Note: all other template.env values were left at their defaults for this
# lab. See docs/lab-steps.md for the full deployment procedure.
